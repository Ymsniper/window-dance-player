# Window Dance Player — Python dependencies
# Install with:
#   pip install -r requirements.txt --break-system-packages
#
# CachyOS / Arch users can pre-install pygame and numpy via pacman:
#   sudo pacman -S python-pygame python-numpy
#   pip install librosa --break-system-packages

librosa>=0.10.0
pygame>=2.5.0
numpy>=1.24.0
